require('dotenv').config(); const fs = require('fs'); const { createClient } = require('@supabase/supabase-js'); process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'; const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_KEY); async function run() { const db = JSON.parse(fs.readFileSync('database.json', 'utf8')); const res1 = await supabase.from('users').upsert(db.users); console.log('users:', res1.error); const res2 = await supabase.from('projects').upsert(db.projects); console.log('projects:', res2.error); const res3 = await supabase.from('features').upsert(db.features); console.log('features:', res3.error); const res4 = await supabase.from('cards').upsert(db.cards); console.log('cards:', res4.error); console.log('Migration complete!'); } run().catch(console.error);
